RUST_LOG=reacher=info
# Copy this file to .env
# ```bash
# cp .env.example .env
# ```
#
# Then tweak the values to match your desired config.

RUST_LOG=reacher=info
SQLX_OFFLINE=true
RCH_WEBDRIVER_ADDR=http://localhost:9515

# ------------------------------------------------------------------------------
# Proxy Configuration (e.g. Bright Data)
# ------------------------------------------------------------------------------
# See docs/self-hosting/proxies/README.md and
# github.com/Oppulence-Engineering/oppulence-paperless-backend/services/lead-scraper-service
# for detailed proxy settings.

# Default proxy (routed via SOCKS5)
# RCH__PROXY__HOST=brd.superproxy.io
# RCH__PROXY__PORT=33335
# RCH__PROXY__USERNAME=brd-customer-<YOUR_CUSTOMER_ID>-zone-<YOUR_ZONE>
# RCH__PROXY__PASSWORD=<YOUR_PASSWORD>

# Helper fields for SMTP HELO/MAIL FROM.
# Ideally these match the RDNS of the proxy IP.
# RCH__HELLO_NAME=reacher.email
# RCH__FROM_EMAIL=hello@reacher.email

